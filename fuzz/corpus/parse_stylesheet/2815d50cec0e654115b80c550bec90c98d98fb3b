pacity-50{
    display: inline-block !important;
  }
  .d-sm-block {
    display: block !importanble-cell {
    display: table-cell !important;
  }
  .d-sm-flexnline: calc(-1 * var(--gap));
    padding-inline: var(--gap);
    b/rder-radius: 0;
  }
  [role=option][aria-selected=true] {
    back-toggle-nub-margin: 2px;
    grid-column: 1;
    grid-row: 1;
    margin: var(--toggle-nub-margin);
    border-radi  border-radius: 0;
  color: var(--e: none;
  [role=optiogle-nub-margin: 2px;
    grid-column: 1;
    grid-row: 1;
    margin: var(--toggle-nub-margin);
    border-radius: 99999rem;
    background: var(--graphical-fg);
  }
  &:checked {
    &::before {
      border-color: var(--accent);
      background: var(--accent);
    }
    &::after {
      transform: translateX(calc(100% + 2 * var(--toggle-nub-margin)));
      background: var(--bg);
    }
  }
  &:indeterminate {
    &::before {
      border: var(--interactive-border-width) var(--interactive-border-style) var(--interactive-bg);
    }
    &::after {
      transform: translateX(calc(50% + vae=menuitem] {
  padding: 0 calc(var(--gap) / 2);
  display: block;
  text-decoration: none;
  border-radius: 0;
  color: var(--e: none;
  [role=optiogle-nub-margin: 2px;
    grid-column: 1;
    grid-row: 1;
    margin: var(--toggle-nub-margin);
    border-radius: 99999rem;
    background: var(--graphical-fg);
  }
  &:checked {
    &::before {
      border-color: var(--accent);
      background: var(--accent);
    }
    &::after {
      transform: translateX(calc(100% + 2 * var(--toggle-nub-margin)));
      background: var(--bg);
    }
  }
  &:indeterminate {
    &::before {
      border: var(--interactive-border-width) var(--ient);
    --temporary-fg}

.bg-prw-reverse {
    fimarylex-direction-subt