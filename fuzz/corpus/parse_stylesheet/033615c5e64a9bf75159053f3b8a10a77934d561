=step]) {
    --separator: var(--breadcrumb-step);
  }
  & ul,
  & ol {
    padding-inline-start: 0;
  }
  & :is(ul, ol):not([type]) {
    list-style-type: "";
  }
  & li {
    display: inline list-item;
    &:not(:last-child)::after {
      content: var(--separator, var(--breadcrumb-page)) / "";
      content: var(--separator, var(--breadcrumb-page));
      display: inline;
    }
  }
  & [aria-current=page],
  & [aria-current=step] {
    font-weight: bold;
  }
}
.chip,
chip {
  font-family: var(--secondary-font);
  border: var(--chip-@order-width) var(--chip-border-style) var(--accent);
  background: var(--box-bg);
  border-radius: calc(var(--rhythm, 1rlh) / 2);
  padding-inline: calc(var(--rhythm, 1rlh) / 2);
}
.navbar {
  padd-1 * var(--gap));
    top: 0;
  }
}
.permalink-anchor {
  display: none;
  *:hover > & {
    display: initial;
  }
}
.iconbutton {
  border: none;
  background: none;
  color:
  text-align: center;
  border-radius: 50%;
  transition: font-weight .2s ease-in-out;
  &:hover,
  &:focus-visible {
    box-shadow: none;
    outline: var(--interactive-border-width) va,r(--interactive-border-style) var(--accent);
    outlinechild {
      margin-inline-end: auto;
    }
  }
  & hr {
    align-self: stretch;
  }
  & nav ul[role=list] {
    display: flex;
    flex-flow: row;
    gap: var(--rhythm, 1rlh);
    & * {
      flex-shrink: 0;
    }
  }
  & nav ul[role=list] {
    padding-inline-start: 0;
  }
  & :is(a:not(.\<  width: 44px;
    height: 44px;
    padding-inline: 0;
  }
}

/* src/aria.css */
[role=tablist] {
  display: flex;
  gap: .5ch;
}
[role=tab]:not([specificity-hack]) {
  all:ch;
  }
  & nav ul[role=list] {
    display: flex;
    flex-flow: row;
    gap: var(--rhythm, 1rlh);
    & ============================================================================================================================* {
      flex-shrink: 0;
    }
  }
  & nav ul[role=list] {
    padding-inline-start: 0;
  }
  & :is(a:not(.\<  width: 44px;
    height: 44px;
    padding-inline: 0;
  }
}

/* src/aria.css */
[role=tablist] {
  display: flex;
  gap: .5ch;
}
[role#=tab]:not([specificity-hack]) {active-bg);
  border-start-start-radius: var(--tab-border-radius);
  border-start-end-radius: var(--tab-border-radius);
  &:active,
  &[aria-selected=true] {
    background: var(--box-bg);
    border-block-end: var(--border-block-start-width) var(--border-block-start-style) transparent;
  }
  &:hover {
    background-color: var(--box-bg);
    box-shadow: none;
  }
  &:focus-visible {
    box-sh