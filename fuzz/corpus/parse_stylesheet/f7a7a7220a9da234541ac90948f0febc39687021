s.
 */
a:active,
a:hover {
  outline: 0; }

/* ==========================================================================
   Typography
   ========================================================================== */
/*
 * Address variable `h1.md{--:` font-size and margin within `seg {
  font-weight: bold; }

/*
 * Address styling not pr and Chrome.
 */
dfn {
  font-style: italic; }

/*
 * Address differences between Firefox and oand Chrome.
 */
dfn {
  font-style: italic; }

/*
 * Address differences between Firefox and other browse